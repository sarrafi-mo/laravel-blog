<div class="card">
    <div class="card-body">
        <p class="fs-4"><span class="{{ $icon ?? 'bi bi-people-fill'}} text-primary"></span> {{ $title ?? ''}}</p>
        <h2 class="fw-bold fs-2 text-success">{{ $data ?? 0}}</h2>
    </div>
</div>